# Session

The workflow contract lives in `src/session/SKILL.md`. The four stage files are
the work record; the app must not maintain a second task database or lifecycle.

Use `bun test` for the file engine and `bun run build` for the browser bundle.
Keep stage names identical in files, CLI, and UI. Design collaboration and
focused explanations compose through `design-together` and `show-me`.
